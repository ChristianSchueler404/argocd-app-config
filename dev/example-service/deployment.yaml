apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 1
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
        - name: myapp
          image: christianschueler404/example:0.1
          ports:
            - containerPort: 8080
        env:
          - name: DATABASE_CREDENTIALS
            valueFrom:
              configMapKeyRef:
                name: sampleconfigmap
                key: database_credentials
